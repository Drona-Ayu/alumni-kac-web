# Absolute origin the site is served from. Only used to build the absolute
# og:image URL, which crawlers require — relative image URLs are unreliable in
# social previews. Override at build time when moving to a custom domain.
VITE_SITE_URL=https://drona-ayu.github.io/alumni-kac-web
